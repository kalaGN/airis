# API Key 账号鉴权：任务

> 状态：草案；依赖 [`20260803-apikey账号鉴权-设计.md`](20260803-apikey账号鉴权-设计.md)
> 完成评审后实施。本文不扩展账号过期、IP 白名单或并发限制。

## 目标与实施边界

将 `/loan` 的现有“`apikey` 仅校验非空 + 全局 secret 验签”替换为：

1. 从内存 -> Redis -> MySQL 按顺序查找账号，未命中、过期或依赖失败时读下一层。
2. 三层都不能返回 60 秒内的新鲜记录时，调用运营中心完整批量账号接口。
3. 只允许 `status == 1` 的账号使用其独立明文 secret 验签。
4. 未知/禁用账号和签名错误返回 HTTP `200`；鉴权系统故障返回 HTTP `503`。
5. 不改变 `/loan` 请求字段、签名拼接格式、Mongo 数据格式和成功响应。

实施期间不运行 `cmd/init_mongo.go`、`cmd/init_mongo.sh` 或其等价数据重建操作。

## 依赖顺序

```text
生产契约/配置门禁
  -> 账号模型与结果语义
      -> 运营中心客户端 -> 独立 Mock 服务
      -> MySQL / Redis / 内存单层存储
          -> 分层查询与批量回源
              -> 签名与日志安全改造
                  -> /loan 鉴权接入
                      -> 启动/关闭接线 -> 集成与发布验证
```

## 阶段一：契约和基础类型

### [ ] 任务 0：确认生产实施门禁

- 依赖：无。
- 可能修改：
  - `doc/specs/20260803-apikey账号鉴权-设计.md`
  - `doc/specs/20260803-apikey账号鉴权-任务.md`
- 验收：
  - 确认真实运营中心是否返回与 Mock 一致的完整批量快照。
  - 确认真实 URL/方法、认证、超时、最大账号数/响应字节数、QPS 和 SLA。
  - MySQL 环境变量使用设计文档已确认的独立配置；继续确认 MySQL 驱动版本、
    Redis/MySQL 安全措施及 L1 容量。
- 验证：`git diff --check`。
- 范围：S（2 个文档）。

### [ ] 任务 1：建立账号模型和分层结果语义

- 依赖：任务 0 对字段契约的确认；可先按 Mock 契约开发。
- 可能修改：
  - `app/models/account.go`
  - `app/models/account_test.go`
  - `app/repositories/account_store.go`
  - `app/repositories/account_store_test.go`
- 验收：
  - `Account` 明确 `account_id/name/apikey/secret/status/refreshed_at/updated_at`，secret 严格限制为 `1..256` 字节。
  - 分层结果可区分 `hit/not_found/stale/dependency_error/invalid_data`，不用空值合并语义。
  - `status != 1` 是有效命中结果，不继续向下层查找。
- 验证：`go test ./app/models ./app/repositories`。
- 范围：M（4 个文件）。

### [ ] 任务 2：实现运营中心批量客户端

- 依赖：任务 1。
- 可能修改：
  - `pkg/operationcenter/types.go`
  - `pkg/operationcenter/validate.go`
  - `pkg/operationcenter/client.go`
  - `pkg/operationcenter/client_test.go`
- 验收：
  - 客户端使用调用方 Context、显式超时和响应 Body 硬上限，不自动重试。
  - 仅接受 HTTP `200` + `code=0` + `complete=true`，校验 `total`、字段长度、时间和唯一键。
  - 任一记录非法、重复 Key、超大响应或不完整快照导致整批失败，不暴露 secret 到错误文本。
- 验证：`go test ./pkg/operationcenter`。
- 范围：M（4 个文件）。

### [ ] 任务 3：实现独立运营中心 Mock 服务

- 依赖：任务 2 的 Schema。
- 可能修改：
  - `pkg/operationcenter/mock_handler.go`
  - `pkg/operationcenter/mock_handler_test.go`
  - `cmd/mock_operation_center/main.go`
  - `.env.example`
- 验收：
  - 独立进程实现 `GET /mock/operation-center/v1/accounts`，不注册到 Airis `routes/api.go`。
  - 固定快照包含一个 `status=1` 和一个 `status=0` 的专用测试账号。
  - 实现 `server_error/timeout/invalid_json/invalid_account/duplicate_apikey/incomplete` 故障注入。
- 验证：`go test ./pkg/operationcenter ./cmd/mock_operation_center`。
- 范围：M（4 个文件）。

## 检查点一：外部契约可联调

- [ ] Mock 和客户端的契约测试通过。
- [ ] 批次非法时没有部分可用数据。
- [ ] 测试输出和错误不包含真实 API Key、secret 或 token。
- [ ] 人工评审 Mock 契约后再开始运行时存储实施。

## 阶段二：配置和三层存储

### [ ] 任务 4：增加鉴权依赖配置

- 依赖：任务 0。
- 实施前现状：本地 `.env` 已写入推荐的 MySQL 变量，使用 `127.0.0.1:3306`、`airis`、
  `airis_auth` 和仅限本地的 `MYSQL_TLS_MODE=disabled`，密码仍为占位值。因为代码尚未解析这些变量，
  本任务不标记完成。
- 可能修改：
  - `pkg/config/app_config.go`
  - `pkg/config/app_config_test.go`
  - `.env.example`
- 验收：
  - MySQL 配置实现 `MYSQL_HOST/PORT/DATABASE/USER/PASSWORD`、TLS、连接/查询/写超时和连接池参数；
    程序在内存中构造 DSN，不增加含密码的 `MYSQL_DSN`。
  - `.env.example` 只同步非敏感占位值和注释；生产示例使用 `MYSQL_TLS_MODE=verify-full`，
    不复制本地 `.env` 密码或 `disabled` TLS 值。
  - 其余配置覆盖运营中心 URL/token/超时/最大响应、L1 容量与三类 TTL。
  - 时长、整数和容量严格解析；非法配置在启动阶段失败，不静默回退。
  - 正向缓存和 MySQL 新鲜度上限不超过 60 秒；运营中心 token 不被日志输出。
- 验证：`go test ./pkg/config`。
- 范围：M（3 个文件）。

### [ ] 任务 5：实现 MySQL 持久化缓存

- 依赖：任务 1、4，以及已评审的 MySQL 驱动。
- 可能修改：
  - `go.mod`
  - `go.sum`
  - `scripts/mysql/001_create_accounts.sql`
  - `app/repositories/account_mysql.go`
  - `app/repositories/account_mysql_test.go`
- 验收：
  - DDL 创建 `accounts`，`apikey`/`account_id` 唯一，`secret VARBINARY(256)`，包含 `refreshed_at`。
  - 按 `apikey` 参数化查询，严格区分命中、未命中和依赖错误。
  - 已完整校验的账号快照在单个事务中批量 upsert，失败时整体回滚。
- 验证：`go test ./app/repositories -run 'TestMySQLAccountStore'`、`go mod verify`。
- 范围：M（5 个文件）。

### [ ] 任务 6：实现 Redis 二级缓存

- 依赖：任务 1、4。
- 可能修改：
  - `app/repositories/account_redis.go`
  - `app/repositories/account_redis_test.go`
- 验收：
  - Key 为 `airis:auth:account:<sha256(apikey)>`，不含明文 API Key。
  - Value 包含版本、账号快照和新鲜度；解码失败按该层错误处理并继续下一层。
  - 所有调用使用请求 Context，不复用现有 `context.Background()` 封装；TTL 不超过 60 秒。
- 验证：`go test ./app/repositories -run 'TestRedisAccountStore'`。
- 范围：S（2 个文件）。

### [ ] 任务 7：实现有界内存一级缓存

- 依赖：任务 1、4。
- 可能修改：
  - `app/repositories/account_memory.go`
  - `app/repositories/account_memory_test.go`
- 验收：
  - 使用 `sha256(apikey)` 作为 Key，支持正向/负向 TTL，并严格限制最大条目数。
  - 并发读写、过期和淘汰无 data race，不启动无法停止的后台 goroutine。
  - L1 命中禁用账号时立即返回，不为寻找启用值而读下层。
- 验证：`go test -race ./app/repositories -run 'TestMemoryAccountStore'`。
- 范围：S（2 个文件）。

### [ ] 任务 8：组合分层查询和批量回源

- 依赖：任务 2、5、6、7。
- 可能修改：
  - `app/repositories/account_layered.go`
  - `app/repositories/account_layered_test.go`
- 验收：
  - 严格实现 L1 -> Redis -> MySQL -> 运营中心，各层未命中、过期或错误均可确定降级。
  - 运营中心返回完整快照后整批尝试写 MySQL，只将当前目标账号回填 Redis/L1；回填失败不改变本次结果。
  - 目标 Key 缺失才写 5 秒负缓存；运营中心失败且无 60 秒内本地记录时 fail closed。
  - 不同 Key 的并发回源共享同一次在途批量刷新，等待请求可被 Context 取消。
- 验证：`go test -race ./app/repositories -run 'TestLayeredAccountStore'`。
- 范围：S（2 个文件）。

## 检查点二：三层降级可验证

- [ ] 每一层的命中、未命中、过期、非法数据和依赖错误都有自动化测试。
- [ ] 运营中心整批校验失败时不会部分回填。
- [ ] 禁用状态最长保留时间不超过 60 秒。
- [ ] `go test -race ./app/repositories ./pkg/operationcenter` 通过。

## 阶段三：安全改造与 `/loan` 接入

### [ ] 任务 9：将签名比较改为常量时间

- 依赖：无，可与任务 2–7 并行。
- 可能修改：
  - `pkg/utils/signature.go`
  - `pkg/utils/signature_test.go`
- 验收：
  - 签名生成格式、MD5 大写输出和参数排序不变。
  - 签名比较使用常量时间函数，正确、错误、大小写和长度异常都有回归测试。
- 验证：`go test ./pkg/utils -run 'TestGenerateSign|TestVerifySign'`。
- 范围：S（2 个文件）。

### [ ] 任务 10：移除请求与响应敏感 Body 日志

- 依赖：无，但必须在鉴权上线前完成。
- 可能修改：
  - `app/middleware/logger.go`
  - `app/middleware/logger_test.go`
- 验收：
  - 默认日志不记录完整请求/响应 Body，不出现 `phone/apikey/sign/secret/token` 原值。
  - 保留方法、路径、HTTP 状态、业务码、耗时和脱敏关联 ID。
  - 不再为日志无界读取并重建请求 Body。
- 验证：`go test ./app/middleware -run 'TestLogger'`。
- 范围：S（2 个文件）。

### [ ] 任务 11：在 Loan Handler 接入账号鉴权

- 依赖：任务 1、8、9、10。
- 可能修改：
  - `app/Http/controllers/loan/index.go`
  - `app/Http/controllers/loan/index_test.go`
  - `app/Http/controllers/loan/dependencies.go`
- 验收：
  - Handler 可注入 `AccountStore` 和贷款查询函数，鉴权先于 Mongo 查询执行。
  - 未知/禁用账号返回 HTTP `200` + `2001`，账号签名错误返回 HTTP `200` + `2002`。
  - 账号存储或运营中心无法安全返回结果时返回 HTTP `503` + `9002`，不回退到全局 secret。
  - 鉴权拒绝或系统故障时断言 Mongo 查询未被调用；成功请求保持现有 JSON 契约。
- 验证：`go test ./app/Http/controllers/loan`。
- 范围：M（3 个文件）。

### [ ] 任务 12：完成启动、路由和资源关闭接线

- 依赖：任务 4–8、11。
- 可能修改：
  - `main.go`
  - `bootstrap/dependencies.go`
  - `bootstrap/route.go`
  - `routes/api.go`
  - `bootstrap/route_test.go`
- 验收：
  - 启动阶段构建 MySQL、Redis、运营中心客户端和分层存储，缺失必填配置时拒绝启动。
  - `/loan` 绑定已注入的 Handler，`GET /health` 不受鉴权影响，Mock 路由不出现在 Airis 主进程。
  - 优雅关闭依次关闭 HTTP Server、MySQL、Redis 和 Mongo，错误被记录但不输出凭据。
  - 主调用链不再使用或回退到 `SECRET_KEY`；是否保留兼容配置字段按任务 0 评审结果执行。
- 验证：`go test ./bootstrap ./routes ./app/Http/controllers/loan`、`go build ./...`。
- 范围：M（5 个文件）。

## 检查点三：主调用链完整

- [ ] 使用 Mock 账号 secret 的合法 `/loan` 请求进入 Mongo 查询。
- [ ] 未知、禁用、错签和依赖故障的 HTTP/业务码与设计一致。
- [ ] 所有鉴权失败路径都不访问 Mongo。
- [ ] 日志、错误和测试输出中不出现明文凭据。

## 阶段四：集成、故障和发布验证

### [ ] 任务 13：增加隔离集成测试

- 依赖：任务 3、5–8、12。
- 可能修改：
  - `tests/integration/account_auth_test.go`
  - `tests/integration/README.md`
  - `tests/integration/test_config.go`
- 验收：
  - 仅使用隔离 MySQL database、Redis DB/Key 前缀和专用 Mock 凭据，不读写生产数据。
  - 覆盖 L1/Redis/MySQL 命中、逐层失败、整批回填、负缓存、60 秒新鲜度和 Mock 故障注入。
  - 未显式提供隔离测试配置时安全跳过，不连接默认或未知数据库。
- 验证：`go test ./tests/integration -count=1`。
- 范围：M（3 个文件）。

### [ ] 任务 14：执行全量质量门禁并同步文档

- 依赖：任务 1–13。
- 可能修改：
  - `README.md`
  - `doc/README.md`
  - `doc/specs/README.md`
  - `doc/specs/20260803-apikey账号鉴权-设计.md`
  - `doc/specs/20260803-apikey账号鉴权-任务.md`
- 验收：
  - 环境变量、Mock 启动、DDL 审核、故障语义、回滚和已知风险与最终实现一致。
  - 设计和任务状态按真实完成情况更新，未验证项继续明确列出。
  - 工作树只包含本需求的最小差异，不夹带无关格式化或清理。
- 验证：

```bash
gofmt -l $(git ls-files '*.go')
go mod verify
go vet ./...
go test ./...
go test -race ./...
go build ./...
go build -o /tmp/airis main.go
git diff --check
```

- 范围：M（5 个文档）。

## 最终检查点

- [ ] `apikey` 存在、`status == 1` 和账号独立 secret 验签均可观察验证。
- [ ] 内存 -> Redis -> MySQL -> 运营中心的命中/降级/故障语义有测试。
- [ ] 账号禁用最迟 60 秒生效，且运营中心故障时不超时使用旧数据。
- [ ] 鉴权失败 HTTP `200`，系统故障 HTTP `503`，响应中不暴露内部错误。
- [ ] 不使用全局 secret 验签或故障回退。
- [ ] API Key、secret、签名、token 和完整手机号不出现在日志、错误或导出文件。
- [ ] 全量格式、静态检查、测试、Race 和构建门禁完成，未运行项如实报告。

## 并行开发与合并顺序

- 任务 2、4、9、10 在任务 1 的公共类型确定后可并行，但共享 Schema 变更必须先同步设计文档。
- 任务 5、6、7 可按统一存储语义并行；任务 8 必须等待三层接口稳定。
- 任务 11 必须在任务 8–10 后合并，任务 12 在 Handler 契约稳定后接线。
- 任务 13 使用已稳定的 Mock 和存储实现，任务 14 最后执行。

## 风险与阻塞项

| 风险/阻塞 | 影响 | 缓解或门禁 |
| --- | --- | --- |
| 真实运营中心不是完整快照 | 不能安全判定 Key 不存在 | 先更新设计为分页/增量契约，不复用当前负缓存语义 |
| 全量快照过大 | 内存、带宽和延迟不可控 | 上线前确认上限并强制 Body 限制，超限 fail closed |
| MySQL 驱动未确认 | 无法完成持久层和启动接线 | 任务 5、12 前完成审核，不自行选版本上线 |
| 明文 secret 持久化 | 数据库/备份读权可伪造签名 | 加密传输、静态/备份加密、最小权限、访问审计和保留周期必须先评审 |
| 旧日志记录完整 Body | API Key、签名和手机号泄露 | 任务 10 是上线前强制门禁 |
| 分层错误被误当未命中 | 误拒绝、缓存污染或绕过鉴权 | 使用显式结果类型，每层覆盖故障测试 |
| 切换后仍回退全局 secret | 未知账号可绕过账号鉴权 | 主链路移除全局 secret 依赖并增加安全回归测试 |

## 回滚边界

- 代码回滚会恢复“API Key 仅校验非空 + 全局 secret”的安全降级行为，必须明确授权，不作为默认故障降级。
- `accounts` 表和 Redis 鉴权 Key 在回滚时保留；Redis Key 按 TTL 自然过期，不执行破坏性删除。
- DDL 是新增表，首期不提供自动 drop 脚本；如需删表必须单独评审数据保留和备份。
